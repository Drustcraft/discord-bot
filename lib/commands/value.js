const Discord = require("discord.js")

function command(interaction) {
	interaction.editReply("Due to a recent update the happened, this command is not avaliable. Please check back, well, frankly, idk when.\n- Project_HSI")
}

module.exports = {
	command: command
}

// CREATED HERE AS BOILER PLATE!
// nomadjimbob pls add endpoint